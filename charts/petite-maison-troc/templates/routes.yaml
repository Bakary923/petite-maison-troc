{{- if .Values.openshift.enabled -}}
# ✅ RESSOURCE SPÉCIFIQUE OPENSHIFT : La Route
# La Route gère automatiquement l'exposition publique, le DNS et le certificat SSL.
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  # Nom de la route pour l'accès externe
  name: petite-maison-frontend
  labels:
    app: petite-maison
    component: frontend
spec:
  # 🔗 LIAISON : On pointe la Route vers le Service Frontend
  to:
    kind: Service
    # Correction : Utilisation du nom dynamique pour correspondre au Service
    name: {{ .Release.Name }}-frontend
  
  # 🚪 PORT : On cible le port du service nommé 'http'
  port:
    targetPort: http

  # 🔐 SÉCURITÉ (HTTPS) : Configuration de la terminaison TLS (Conformité DevSecOps)
  tls:
    # Mode 'edge' : Le certificat SSL est géré par le routeur OpenShift (Terminaison TLS).
    termination: edge
    
    # Redirection automatique : Force l'utilisateur à passer du HTTP vers le HTTPS.
    insecureEdgeTerminationPolicy: Redirect
{{- end -}}